import React from "react";
import "./FetchPokeData.css";

const FetchPokeData = () => {
  return (
    <div className="btn-container">
      <button className="fetch-btn">Fetch Pokemon</button>
    </div>
  );
};

export default FetchPokeData;
